---
name: Release
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:
  build:
    uses: ThingsO2/monom-ci-workflows/.github/workflows/release-workflow.yaml@main
    secrets: inherit
